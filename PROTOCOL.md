# MASTER WORK PROTOCOL — البروتوكول الموحد للعمل المستقبلي
> مستخلص من الأنماط الفعلية المطبقة عبر مشاريعك (winner druge / lead-intel / ai studio / pharmacy / portfolio)
> المبادئ: Distilled from real work, not theory.

---

## المستوى 1 — النواة المشتركة (تُطبَّق في كل مشروع بلا استثناء)

هذه هي القواعد الفعلية المتبعة عبر مشاريعك، موحّدة مهما اختلفت أسماء الملفات:

| # | القاعدة | اسم الملف (أياً كان) |
|---|---------|----------------------|
| 1 | اقرأ AGENTS.md أولاً قبل أي إجراء | `AGENTS.md` (ثابت في كل مكان) |
| 2 | اقرأ حالة المشروع قبل البدء | `PROGRESS.md` / `MEMORY.md` / `CURRENT_STATE.md` |
| 3 | اقرأ نقطة الاستئناف | `NEXT_STEPS.md` / `NEXT_SESSION.md` |
| 4 | اقرأ سجل القرارات قبل أي اقتراح | `DECISIONS.md` / `decision-log.md` |
| 5 | لا كود قبل Spec → Plan → Tasks | `specs/spec.md`, `plan.md`, `tasks.md` أو `TASKS.md` |
| 6 | Read-Before-Write: اقرأ الملف قبل تعديله | (أي ملف) |
| 7 | تتبّع المهام: TODO → IN_PROGRESS → DONE | `TASKS.md` / `specs/tasks.md` |
| 8 | وثّق كل قرار مهم (السبب + التأثير + التاريخ) | `DECISIONS.md` |
| 9 | جراحة دقيقة: لا تلمس إلا المطلوب فقط | (Surgical Editing) |
| 10 | حدّث الذاكرة قبل إنهاء أي جلسة | `PROGRESS.md` + `NEXT_STEPS.md` + `DECISIONS.md` |
| 11 | Commit بعد كل وحدة عمل مكتملة | Git (تاريخ نظيف + رجوع سهل) |
| 12 | تواصل بنفس لغة المستخدم | عربي/إنجليزي حسب المستخدم |

**دورة العمل (مقتبسة من WORKFLOW.md الفعلي):**
```
تحميل الذاكرة → تحليل → أسئلة مفتوحة → Spec → قبول → Plan → Tasks → تنفيذ
→ مراجعة → اختبار → توثيق → تحديث الذاكرة → Commit
```

---

## المستوى 2 — استبيان التصنيف (سؤال واحد في كل مرة حتى قرار دقيق)

قبل بدء أي مشروع، أجب عن هذه الأسئلة بالترتيب — كل إجابة تُحدد الطبقة المضافة:

1. **نوع المشروع؟**
   - (A) موقع ثابت/صفحة تسويقية
   - (B) تطبيق ويب مع قاعدة بيانات
   - (C) نظام ERP / منصة AI / تطبيق سطح مكتب متكامل
2. **هل يوجد قاعدة بيانات؟** (نعم/لا + المحرك: SQLite/Firebase/Firebird/Postgres…)
3. **هل يوجد API خلفي؟** (نعم/لا + عدد تقريبي للملفات/النقاط)
4. **هل يوجد Auth/RBAC أو بيانات مالية أو عمليات تحتاج Audit؟**
5. **هل يُبنى بأكثر من أداة/وكيل AI؟** (OpenCode + Cursor + Codex + موديلات متعددة)
6. **أين يُنشر؟** (Cloudflare Pages / VPS / Docker / محلي / مزيج)
7. **هل يحتاج Backups خارجية؟** (ما أهمية فقدان البيانات؟)

**قاعدة القرار:** أسأل سؤالاً واحداً، أنتظر جوابك، ثم أجيب السؤال التالي — لا أفترض.

---

## المستوى 3 — الطبقات حسب التصنيف

### 🔵 طبقة بسيطة (A — موقع ثابت)
```
/AGENTS.md            ← القواعد (نفس النواة)
/README.md            ← البنية والتشغيل
/PROGRESS.md, NEXT_STEPS.md, DECISIONS.md
/SECURITY.md          ← header/أسرار إن وُجد
/DEPLOYMENT.md        ← إن نُشر سحابياً
/CHANGELOG.md         ← سجل التغييرات
```
لا تُضاف: DATABASE_MAP, API_CONTRACTS, migrations, tests التفصيلية.

### 🟠 طبقة متوسطة (B — تطبيق بقاعدة بيانات)
كل ما سبق +:
```
/docs/ARCHITECTURE.md     ← الوحدات + تدفق البيانات
/docs/DATABASE_MAP.md     ← جداول + علاقات + فهارس
/docs/API_CONTRACTS.md    ← DTO + أخطاء + صلاحيات (إن وُجد API)
/docs/CHANGELOG.md        ← سجل التغييرات بالتواريخ
/TASKS.md                 ← مهام بمعايير قبول (Acceptance)
/tests/                   ← Unit لكل منطق حرج
/COMMON_LANGUAGE.md       ← (إن تعاون أكثر من موديل AI — مثال: pharmacy)
```

### 🔴 طبقة انتربرايز (C — ERP/منصة AI/متكامل)
كل ما سبق + بنية `.ai-framework/` كاملة (مثل winner druge فعلياً):
```
.ai-framework/
├── AGENTS.md        ← الهوية + التسلسل الإلزامي (اقرأ أولاً)
├── CONSTITUTION.md  ← المبادئ الثابتة + معايير الكود + الممنوعات
├── WORKFLOW.md      ← دورة حياة الميزة + قوالب spec/plan/tasks/handoff
├── CONTEXT.md       ← إدارة امتلاء السياق
├── SUBAGENTS.md     ← توزيع الأدوار بين الوكلاء
├── MODELS.md        ← أي نموذج AI يعمل على أي جزء
├── GIT_RULES.md     ← سياسة الـ commit/branch
├── TESTING.md       ← معايير الاختبار
├── SECURITY.md      ← OWASP + أسرار + Audit
├── QUALITY.md       ← Definition of Done
├── REVIEW.md        ← مراجعة الكود
├── DECISIONS.md     ← سجل القرارات
├── PROGRESS.md      ← الحالة الحالية
├── NEXT_SESSION.md  ← نقطة الاستئناف
└── CHANGELOG.md     ← سجل التغييرات التفصيلي
memory/
├── lessons-learned.md
├── patterns.md
└── fallback-strategies.md   ← استراتيجيات التعافي عند فشل المكونات
specs/
├── spec.md / plan.md / tasks.md  (+ قوالب)
backups/            ← نسخ خارج git
```

### 🟣 بنية صامدة (Resilience) — مشتركة لكل المشاريع
```
لكل مشروع يعتمد خدمات خارجية/نماذج AI:
1. حدد مكونات الإخفاق (LLM، API خارجي، DB، GPU)
2. وثّقFallbackstrategy لكل مكون:
   - LLM لا يعمل → JSON افتراضي أو موديل أصغر
   - GPU لا يعمل → Pillow/FFmpeg/static fallback
   - DB لا يعمل → Local storage + مزامنة لاحقة
   - API خارجي لا يعمل → Timeout + retry + بديل
3. كل خطوة pipeline يجب أن تُعالج الخطأ بدلاً من التعطل
```

---

## المستوى 4 — برومبت بداية المشروع (انسخه والصقه عند أي مشروع جديد)

```
أنت تعمل وفق MASTER WORK PROTOCOL.

[المشروع]: <وصف المشروع — ماذا نبني؟ لمن؟ ما المشكلة؟>

قبل أي كتابة كود:
1) اقرأ AGENTS.md ثم PROGRESS.md و NEXT_STEPS.md و DECISIONS.md
   إن وُجدت في هذا المسار.
2) صنّف المشروع عبر الاستبيان التالي — سؤالاً واحداً في كل مرة:
   - النوع؟ (ثابت / ويب+DB / ERP أو منصة AI)
   - قاعدة بيانات؟ بأي محرك؟
   - API؟ auth/RBAC؟ بيانات مالية؟
   - أبنى بأداة واحدة أم عدة وكلاء؟
   - أين النشر؟ وهل نحتاج نسخ احتياطي؟
3) لا تبدأ التنفيذ حتى نتفق على: Spec معتمدة + خطة (plan) + قائمة مهام (tasks)
   مع معايير قبول لكل مهمة.
4) بعد كل وحدة عمل: راجع → اختبر → حدّث PROGRESS/NEXT_STEPS/DECISIONS → Commit.
5) لا تُضِف أي ملفات/توثيق لا يحتاجها تصنيف المشروع (KISS).
6) عند نهاية الجلسة: أكمل NEXT_STEPS.md بما سيُعمل بعدين.
```

---

## تعريف "مكتمل" (مقتبس من عملك الفعلي)
- [ ] Spec معتمدة قبل التنفيذ
- [ ] الكود يعمل (build/run ناجح)
- [ ] اختبار للمنطق الحرج (حسب التصنيف)
- [ ] RTL عربي سليم إن كان UI عربي
- [ ] لا secrets في الكود
- [ ] الذاكرة محدّثة (PROGRESS + NEXT_STEPS + DECISIONS)
- [ ] Commit بعد الوحدة

---

## المستوى 5 — وضع التدريب والشرح (Mentoring Mode)
> يُفعَّل عندما يطلب المستخدم الشرح التعليمي أثناء العمل

### ماذا يعني؟
عندما تكون في وضع التدريب، يعمل المساعد كـ **مُرَشّد/مُدرب شخصي**:

1. **قبل كل خطوة:** اشرح ماذا ستفعل ولماذا
2. **أثناء التنفيذ:** وضّح كل أمر أو سطر كما لو كنت أمام طالب
3. **بعد كل خطوة:** اسأل المستخدم:
   - "هل هذا واضح؟"
   - "هل تريد تفصيل أكثر؟"
   - "أو ننتقل للخطوة التالية؟"
4. **لا تتخطى شرحاً حتى يقول المستخدم "كفي" أو "تمام، الوضع التالي"**

### أفواز التدريب (Tiered Explanation)
| الطلب | الكثافة |
|-------|---------|
| "اشرح لي" | شرح تفصيلي كامل لكل حرف |
| " KGsimple" | شرح مختصر النقطة الرئيسية |
| "lead" | شرح متوسط مع مثال سريع |
| "CH" | شرح عالي الكثافة، مع كل why/how/mistake |

### قاعدة الانتهاء
```
المستخدم: "كفي" → ننتقل للخطوة التالية
المستخدم: "تمام" → نختصر الشرح في المرة القادمة
المستخدم: "أعد الشرح" → نعيد من البداية
المستخدم: " searched" → نتوقف عن الشرح وننفّذ مباشرة
```

---

*هذا الملف قابل للتعديل عبر قرار موثق في DECISIONS.md فقط*